# 云服务商测评网站设计规范

## 设计理念

**"专业、清晰、可信赖"** - 采用深蓝紫渐变作为主色，传达前沿科技与专业可靠的双重属性；通过精心调配的辅助色和中性色，构建层次分明、易于阅读的界面，让技术决策者能够快速获取关键信息。

---

## 色系系统

### 主色 (Primary Color)

- **Primary-50**: `#EEF2FF` - 极浅蓝紫，用于背景、卡片背景、高亮区域
- **Primary-500**: `#6366F1` - 亮靛蓝，用于次要按钮、图标、装饰元素
- **Primary-600**: `#4F46E5` - 靛蓝，用于主要按钮、链接、品牌标识、重要操作
- **Primary-700**: `#4338CA` - 深靛蓝，用于按钮悬停状态、强调元素

**应用场景**:
- 头部导航栏：使用 `bg-gradient-to-r from-primary-600 to-primary-700`
- 主要按钮：`bg-primary-600 text-white hover:bg-primary-700`
- 次要按钮：`bg-white text-primary-600 border-primary-600 hover:bg-primary-50`
- 链接：`text-primary-600 hover:text-primary-700`
- 表格表头：`bg-primary-50 text-primary-700`

### 辅助色 (Secondary Colors)

#### 成功色 (Success)
- **Success-50**: `#ECFDF5` - 浅绿背景
- **Success-600**: `#10B981` - 翠绿，用于成功提示、完成状态、正向指标

**应用场景**:
- 成功消息：`bg-success-50 text-success-600`
- 状态标签（运行中）：`bg-success-50 text-success-600`
- 正向指标：`text-success-600`

#### 警告色 (Warning)
- **Warning-50**: `#FFFBEB` - 浅琥珀背景
- **Warning-500**: `#F59E0B` - 琥珀，用于警告提示、注意事项、待处理状态

**应用场景**:
- 警告消息：`bg-warning-50 text-warning-500`
- 警告标签：`bg-warning-50 text-warning-500`

#### 错误色 (Error)
- **Error-50**: `#FEF2F2` - 浅红背景
- **Error-600**: `#DC2626` - 红色，用于错误提示、失败状态、删除操作

**应用场景**:
- 错误消息：`bg-error-50 text-error-600`
- 错误标签：`bg-error-50 text-error-600`

#### 信息色 (Info)
- **Info-50**: `#EFF6FF` - 浅蓝背景
- **Info-500**: `#3B82F6` - 蓝色，用于信息提示、帮助说明、中性状态

**应用场景**:
- 信息消息：`bg-info-50 text-info-500`
- 信息标签：`bg-info-50 text-info-500`

### 中性色 (Neutral Colors)

- **Neutral-50**: `#F9FAFB` - 近白灰，用于页面背景、容器背景
- **Neutral-100**: `#F3F4F6` - 极浅灰，用于卡片背景、输入框背景、次要区域背景
- **Neutral-300**: `#D1D5DB` - 浅灰，用于边框、分割线、禁用状态
- **Neutral-500**: `#6B7280` - 中灰，用于辅助文字、说明文字、占位符
- **Neutral-700**: `#374151` - 中深灰，用于副标题、次要文字
- **Neutral-900**: `#111827` - 深灰黑，用于主标题、重要文字

**应用场景**:
- 主标题：`text-neutral-900`
- 副标题：`text-neutral-700`
- 辅助文字：`text-neutral-500`
- 边框：`border-neutral-300`
- 背景：`bg-neutral-50` 或 `bg-neutral-100`
- 禁用状态：`bg-neutral-300 text-neutral-500`

---

## 组件设计规范

### 头部 (Header)
```tsx
<header className="bg-gradient-to-r from-primary-600 to-primary-700 text-white shadow-lg">
  <h1 className="text-3xl font-bold">标题</h1>
  <p className="text-primary-50 opacity-90">副标题</p>
</header>
```

### 按钮系统

#### 主要按钮 (Primary Button)
```tsx
<button className="bg-primary-600 text-white px-4 py-2 rounded-lg hover:bg-primary-700 transition-colors">
  主要操作
</button>
```

#### 次要按钮 (Secondary Button)
```tsx
<button className="bg-white text-primary-600 border border-primary-600 px-4 py-2 rounded-lg hover:bg-primary-50 transition-colors">
  次要操作
</button>
```

#### 成功按钮
```tsx
<button className="bg-success-600 text-white px-4 py-2 rounded-lg hover:opacity-90 transition-opacity">
  成功操作
</button>
```

#### 危险按钮
```tsx
<button className="bg-error-600 text-white px-4 py-2 rounded-lg hover:opacity-90 transition-opacity">
  危险操作
</button>
```

### 卡片 (Card)
```tsx
<div className="bg-white rounded-xl shadow-md p-6 border border-neutral-300">
  {/* 内容 */}
</div>
```

### 表格 (Table)

#### 表头
```tsx
<thead className="bg-primary-50">
  <th className="px-6 py-3 text-left text-xs font-semibold text-primary-700 uppercase">
    列名
  </th>
</thead>
```

#### 表格行
```tsx
<tbody className="bg-white divide-y divide-neutral-300">
  <tr className="hover:bg-primary-50 transition-colors">
    <td className="px-6 py-4 text-neutral-700">内容</td>
  </tr>
</tbody>
```

### 标签 (Badge/Tag)

#### 状态标签
```tsx
{/* 成功状态 */}
<span className="px-2 py-1 text-xs font-medium bg-success-50 text-success-600 rounded-full">
  运行中
</span>

{/* 警告状态 */}
<span className="px-2 py-1 text-xs font-medium bg-warning-50 text-warning-500 rounded-full">
  警告
</span>

{/* 错误状态 */}
<span className="px-2 py-1 text-xs font-medium bg-error-50 text-error-600 rounded-full">
  错误
</span>

{/* 信息状态 */}
<span className="px-2 py-1 text-xs font-medium bg-info-50 text-info-500 rounded-full">
  信息
</span>
```

#### 大洲标签
```tsx
<span className="px-2 py-1 text-xs font-medium bg-primary-50 text-primary-700 rounded-full">
  亚洲
</span>
```

### 输入框 (Input)
```tsx
<input 
  className="px-3 py-2 border border-neutral-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 bg-white text-neutral-900 placeholder:text-neutral-500"
  placeholder="请输入..."
/>
```

### 统计卡片 (Stats Card)
```tsx
<div className="bg-white rounded-xl shadow-md hover:shadow-xl transition-all p-6 border border-neutral-300">
  <p className="text-neutral-500 text-sm font-medium mb-2">标签</p>
  <p className="text-3xl font-bold bg-gradient-to-r from-primary-500 to-primary-600 bg-clip-text text-transparent">
    数值
  </p>
</div>
```

---

## 页面布局规范

### 页面背景
```tsx
<div className="min-h-screen bg-gradient-to-br from-primary-50 via-neutral-50 to-primary-50">
  {/* 内容 */}
</div>
```

### 加载状态
```tsx
<div className="flex items-center justify-center">
  <div className="inline-block animate-spin rounded-full h-12 w-12 border-b-2 border-primary-600"></div>
  <p className="text-neutral-500 ml-4">加载中...</p>
</div>
```

### 页脚
```tsx
<footer className="bg-neutral-900 text-white py-8 mt-12">
  <p className="text-neutral-400 text-sm">版权信息</p>
  <p className="text-neutral-500 text-xs mt-2">说明文字</p>
</footer>
```

---

## 数据可视化色板

用于图表中区分不同云服务商的颜色：

- **AWS**: `#FF9900` (橙色)
- **Azure**: `#0078D4` (蓝色)
- **Google Cloud**: `#4285F4` (亮蓝)
- **阿里云**: `#FF6A00` (橙红)
- **腾讯云**: `#9C27B0` (紫色)
- **华为云**: `#D0021B` (红色)
- **火山引擎**: `#00BCD4` (青色)
- **Oracle Cloud**: `#C74634` (红棕)
- **IBM Cloud**: `#FFC107` (金色)
- **OVH Cloud**: `#607D8B` (蓝灰)
- **DigitalOcean**: `#E91E63` (粉红)

**注意**: 在图表中使用时，建议在浅色背景上使用深色边框或阴影，确保可读性。

---

## 可访问性要求

### 对比度标准
- 所有颜色组合必须满足 **WCAG 2.1 AA级对比度标准**
- 正文文字 (Neutral-900) 与背景 (White): 16.8:1 ✅ AAA级
- 主色按钮 (Primary-600) 与文字 (White): 7.1:1 ✅ AA级
- 次要文字 (Neutral-500) 与背景 (White): 4.5:1 ✅ AA级

### 色盲友好
- 不仅依赖颜色传达信息，同时使用图标、文字、形状区分
- 图表中使用不同图案/纹理辅助区分
- 确保所有交互元素有明确的视觉反馈

### 交互状态
- 所有可点击元素提供明确的悬停、聚焦、激活状态
- 使用颜色 + 边框 + 阴影的组合增强可见性

---

## 代码规范

### 使用 Tailwind CSS 类名
- 优先使用 Tailwind CSS 工具类，而不是内联样式
- 使用已定义的颜色变量（如 `primary-600`、`neutral-500` 等）
- 避免使用硬编码的颜色值

### 响应式设计
- 使用 Tailwind 的响应式前缀（`sm:`、`md:`、`lg:`、`xl:`）
- 确保在所有设备上保持良好的可读性和可用性

### 一致性
- 相同功能的组件使用相同的样式
- 保持间距、圆角、阴影等视觉元素的一致性
- 使用统一的字体大小和行高

---

## 禁止事项

❌ **不要使用**:
- 硬编码的颜色值（如 `#FF0000`），应使用定义的颜色变量
- 未定义的颜色类名（如 `bg-red-500`），应使用 `bg-error-600`
- 不一致的圆角大小（统一使用 `rounded-lg` 或 `rounded-xl`）
- 不一致的阴影（统一使用 `shadow-md` 或 `shadow-lg`）

✅ **应该使用**:
- 定义好的颜色系统（`primary-*`、`neutral-*`、`success-*` 等）
- 统一的间距系统（Tailwind 的间距工具类）
- 一致的组件样式模式

---

## 示例代码

### 完整的组件示例
```tsx
// 统计卡片组件
<div className="bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 p-6 border border-neutral-300">
  <div className="flex items-center justify-between">
    <div>
      <p className="text-neutral-500 text-sm font-medium mb-2">云服务商</p>
      <p className="text-3xl font-bold bg-gradient-to-r from-primary-500 to-primary-600 bg-clip-text text-transparent">
        11
      </p>
    </div>
    <div className="w-12 h-12 rounded-lg bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center text-2xl">
      ☁️
    </div>
  </div>
</div>
```

---

## 总结

遵循以上设计规范，确保：
1. **视觉一致性** - 所有组件使用统一的色系和样式
2. **专业感** - 深蓝紫主色传达专业、可靠的形象
3. **可访问性** - 满足 WCAG 2.1 AA 级标准
4. **可维护性** - 使用统一的 Tailwind 类名，便于维护和更新

**记住**: 当创建新组件或修改现有组件时，始终参考此规范，保持整体设计风格的一致性。

